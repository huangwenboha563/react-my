import React from 'react';
import PropTypes from 'prop-types';

const MyComponent = props => {
	return (
		<div>

		</div>
	);
};

MyComponent.propTypes = {

};

export default MyComponent;


// 创建一个函组件的快捷键盘是  rsc  |  rscp